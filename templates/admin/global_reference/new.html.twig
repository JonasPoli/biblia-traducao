{% extends 'admin/base.html.twig' %}

{% block title %}Nova Referência Global{% endblock %}

{% block body %}
    <div class="mb-6">
        <a href="{{ path('app_global_reference_index') }}" class="text-slate-500 hover:text-slate-700 dark:text-slate-400 dark:hover:text-slate-200 flex items-center gap-2">
            <sl-icon name="arrow-left"></sl-icon> Voltar para lista
        </a>
    </div>

    <div class="bg-white dark:bg-slate-800 rounded-lg shadow p-6 max-w-3xl mx-auto">
        <h1 class="text-2xl font-bold text-slate-800 dark:text-white mb-6">Nova Referência Global</h1>

        <form method="post">
            {% include 'admin/global_reference/_form.html.twig' %}
        </form>
    </div>
{% endblock %}
