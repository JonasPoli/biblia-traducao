<!DOCTYPE html>
<html lang="{{ app.request is defined and app.request ? app.request.locale : 'en' }}">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
        <title>{% block title %}Site Base{% endblock %}</title>
        <link rel="icon" href="">

        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
        {% endblock %}

        {% block javascripts %}
            <script src="{{ asset('app.js') }}" defer></script>
        {% endblock %}
    </head>
    <body class="min-h-screen bg-white text-slate-900 antialiased dark:bg-slate-950 dark:text-slate-100">
        <div class="flex min-h-screen flex-col">
            <header class="border-b border-slate-200/60 bg-white/90 backdrop-blur dark:border-white/10 dark:bg-slate-950/90">
                <div class="mx-auto flex max-w-7xl items-center justify-between px-6 py-4 lg:px-8">
                    <div class="flex items-center gap-3">
                        <div class="flex h-10 w-10 items-center justify-center rounded-2xl bg-gradient-to-br from-sky-400 to-cyan-300 text-base font-semibold text-slate-900">
                            SB
                        </div>
                        <div>
                            <p class="text-sm font-semibold uppercase tracking-[0.3em] text-slate-500 dark:text-slate-300">site base</p>
                            <p class="text-sm text-slate-500 dark:text-slate-400">Tailwind + Symfony starter</p>
                        </div>
                    </div>
                    
                    <nav class="hidden md:flex items-center gap-6 text-sm font-medium text-slate-600 dark:text-slate-300">
                        <a href="{{ path('app_dashboard') }}" class="hover:text-slate-900 dark:hover:text-white transition-colors">Dashboard</a>
                        <a href="{{ path('app_global_reference_index') }}" class="hover:text-slate-900 dark:hover:text-white transition-colors">Ref. Globais</a>
                    </nav>

                    <div class="flex items-center gap-2 text-xs font-medium">
                        <button type="button" onclick="lightMode()" class="rounded-full border border-slate-900/10 px-3 py-1 text-slate-600 transition hover:border-slate-900/30 hover:text-slate-900 dark:border-white/10 dark:text-slate-300 dark:hover:border-white/30 dark:hover:text-white">
                            Claro
                        </button>
                        <button type="button" onclick="darkMode()" class="rounded-full border border-slate-900/10 px-3 py-1 text-slate-600 transition hover:border-slate-900/30 hover:text-slate-900 dark:border-white/10 dark:text-slate-300 dark:hover:border-white/30 dark:hover:text-white">
                            Escuro
                        </button>
                    </div>
                </div>
            </header>

            <main class="flex-1">
                {% block body %}{% endblock %}
            </main>

            <footer class="border-t border-slate-200/60 bg-white/80 dark:border-white/10 dark:bg-slate-950/80">
                <div class="mx-auto flex max-w-7xl flex-col gap-2 px-6 py-6 text-xs text-slate-600 sm:flex-row sm:items-center sm:justify-between lg:px-8 dark:text-slate-400">
                    <p>&copy; {{ "now"|date("Y") }} Site Base. Todos os direitos reservados.</p>
                    <p class="text-slate-500 dark:text-slate-500">Tailwind compilado via <code class="rounded bg-slate-900/80 px-2 py-0.5 text-[11px] text-white dark:bg-white/5">php bin/console tailwind:build</code></p>
                </div>
            </footer>
        </div>
    </body>
</html>
